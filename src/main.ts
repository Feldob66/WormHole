import bcModSDK from 'bondage-club-mod-sdk';

const VERSION = "0.0.0.1";
const NAME = "wormhole";
const NICKNAME = "Wormhole';

const WORMHOLE = bcModSDK.registerMod({
    name: NICKNAME,
    fullname: NAME,
    version: VERSION,
    repostitory: "",
});

